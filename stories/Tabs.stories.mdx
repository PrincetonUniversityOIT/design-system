import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

<Meta title="Components/Tabs" component="Tabs" />

export const Template = (args) => html`
    <div class="jazz-tablist" role="tablist">
      <button role="tab" id="one">Tab Label A</button>
      <button role="tab" id="two" aria-selected="true">Tab Label B</button>
      <button role="tab" disabled>Disabled Button</button>
      <button role="tab">Groups</button>
      <button role="tab">Funding</button>
      <button role="tab">Expenses</button>
      <button role="tab" aria-controls="icons">Icon</button>
    </div>
    <div id="icons">
        Hello, Icons!
    </div>
    <script id="initscript">
      PrincetonDesignSystem.enableDesignSystem();
    </script>
`;

export const AutoActivateTemplate = (args) => html`
    <div class="jazz-tablist jazz-auto-activate" role="tablist">
      <button role="tab" id="one">Tab Label A</button>
      <button role="tab" id="two" aria-selected="true">Tab Label B</button>
      <button role="tab" disabled>Disabled Button</button>
      <button role="tab">Groups</button>
      <button role="tab">Funding</button>
      <button role="tab">Expenses</button>
      <button role="tab" aria-controls="icons">Icon</button>
    </div>
    <div id="icons">
        Hello, Icons!
    </div>
    <script id="initscript">
      PrincetonDesignSystem.enableDesignSystem();
    </script>
`;

export const MissingRolesTemplate = (args) => html`
    <div class="jazz-tablist">
      <button id="one">Tab Label A</button>
      <button id="two" aria-selected="true">Tab Label B</button>
      <button disabled>Disabled Button</button>
      <button>Groups</button>
      <button>Funding</button>
      <button>Expenses</button>
      <button aria-controls="icons">Icon</button>
    </div>
    <div id="icons">
        Hello, Icons!
    </div>
    <script id="initscript">
      PrincetonDesignSystem.enableDesignSystem();
    </script>
`;

# Tabs

The following section demonstrates a simple set of tabs:

<Canvas>
    <Story name="Simple" args={{
        label: 'Simple Tabs' 
    }}>
      {Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Auto Activate" args={{
        label: 'Auto Activate Tabs' 
    }}>
      {AutoActivateTemplate.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="Missing Roles" args={{
        label: 'Missing Roles' 
    }}>
      {MissingRolesTemplate.bind({})}
    </Story>
</Canvas>

Some general considerations:

* ARIA guidance was not completely followed because it actually decreases usability (https://simplyaccessible.com/article/danger-aria-tabs/)
* Ordered list (ol tag) and navigation (nav tag) containers were used for semantic purposes. 

TODO: 

* Hover/focus color for tabs that are not selected needs to be improved.
* Allowing labels to break onto multiple lines if that is desired
* Tabs that use icons and/or are completely made up of icons.
* Scrolling of the tabs panel (perhaps can simply create a scrolling panel)
* Javascript events?
